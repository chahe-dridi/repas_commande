{% extends '/backOffice/base.html.twig' %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ commande.prix }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ commande.etat }}</td>
            </tr>
            <tr>
                <th>IdRepas</th>
                <td>{{ commande.idRepas }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_admin_index') }}">back to list</a>

    <a href="{{ path('app_commande_admin_edit', {'id': commande.id}) }}">edit</a>

    {{ include('commande_admin/_delete_form.html.twig') }}
{% endblock %}
